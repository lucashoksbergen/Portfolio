sessionStorage.setItem(
  "Underwater Cooperation",
  "placeholdertext describing the project.aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa"
);

sessionStorage.setItem(
  "AirBar Planet Infographic",
  "placeholdertext describing the project."
);

sessionStorage.setItem(
  "Monster Hunter World:Iceborne Buildcrafting tool",
  "placeholdertext describing the project."
);

sessionStorage.setItem(
  "SeGa Imagineers",
  "placeholdertext describing the project."
);

sessionStorage.setItem(
  "Hexagon Game",
  "placeholdertext describing the project."
);

sessionStorage.setItem(
  "Elemental Escape",
  "placeholdertext describing the project."
);

sessionStorage.setItem(
  "Project Mafia",
  "placeholdertext describing the project."
);

sessionStorage.setItem(
  "Unity stealth AI tech test",
  "placeholdertext describing the project."
);

sessionStorage.setItem(
  "Plastic Disaster",
  "placeholdertext describing the project."
);

sessionStorage.setItem(
  "Arachnophobia Simulator",
  "placeholdertext describing the project."
);

sessionStorage.setItem(
  "Op Jezelf App",
  "placeholdertext describing the project."
);
